1. getting parameters
2. validating them
3. init quantum compiler -> init dongles -> init coders
4. run as many threads as coders plus one monitor thread
5. worker function does:
        - looping through activities and checks broadcast before every loop or activity?
		- 
6. monitor does waiting for a signal and as soon as it received it broadcasts it to every thread
7. 


how to implement dongle cd?
coder has to release the dongle and after that nobody can touch it


extra thread for monitor
it waits for the signal of the coder burnout and then it broadcasts all others to stop


check thread_create return - EVERYWHERE!!!


https://valgrind.org/docs/manual/hg-manual.html
POSIX requires that implementations of standard I/O (printf, fprintf, fwrite, fread, etc) are thread safe.
Unfortunately GNU libc implements this by using internal locking primitives that Helgrind is unable to intercept.
Consequently Helgrind generates many false race reports when you use these functions.

Helgrind attempts to hide these errors using the standard Valgrind error-suppression mechanism.
So, at least for simple test cases, you don't see any.
Nevertheless, some may slip through. Just something to be aware of.



